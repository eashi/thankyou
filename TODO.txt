- Redesign of application exist based on exception resulting of creating the PR (line 151)
- Write the contributors list to a persisted storage (decided later) before a crash happens. Something like a file on the file system, or in the cloud :P.
- Upgrade to dotnet 5.0, and use Records for the classes that just holds data.
- Create a `!todo` command to show all the todo items in the TODO.txt file, And/Or show all the //TODO comments in the repo
- Melbshaker: in the future, maybe we can enhance it so the credit includes the suggestion/help they provided.
- Give the moderator an example if the error whisper to make it easy for them.
- Revise the empty catch exception in line 151. 
- Move to using Dependency Injection from the beginning.